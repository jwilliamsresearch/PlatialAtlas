import { FeatureCollection } from 'geojson';

export default function H3Overlay(_props: { data: FeatureCollection | null }) {
  // Rendering is handled in MapShell via MapLibre layer.
  return null;
}

